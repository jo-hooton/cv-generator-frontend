import React from 'react';

const CVHeader = (props) => {
    return (
        <div>
            <h1>
            {props.title}
            </h1>
            Photo
        </div>
    )
}

export default CVHeader