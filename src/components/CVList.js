import React from 'react';
import CVListItem from './CVListItem'

const CVList = (props) => {
    return (
        <div>
            List
            < CVListItem 
            // list={this.props.list} 
            />
        </div>
    )
}

export default CVList